{
  "name": "@nominate/app-nomination-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "dev": "echo 'nomination-worker dev entry TBD'"
  },
  "dependencies": {
    "@nominate/configuration": "workspace:*",
    "@nominate/contracts": "workspace:*",
    "@nominate/domain": "workspace:*",
    "@nominate/observability": "workspace:*",
    "@nominate/persistence": "workspace:*",
    "@nominate/slack": "workspace:*"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.143"
  }
}
