{
  "name": "nominate-slackbot",
  "version": "0.0.0",
  "private": true,
  "description": "Internal Slack employee-recognition bot. See docs/ for authoritative behavior.",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "pnpm -r --parallel lint",
    "typecheck": "tsc -b",
    "test": "vitest run --project unit --passWithNoTests",
    "test:integration": "vitest run --project integration --passWithNoTests",
    "build": "pnpm -r --parallel build",
    "build:lambda": "pnpm --filter \"@nominate/app-*\" --parallel build:lambda",
    "verify": "pnpm lint && pnpm typecheck && pnpm test && pnpm build",
    "prepare": "husky",
    "dev": "pnpm -r --parallel --stream dev",
    "infra:validate": "bash scripts/infra-validate.sh",
    "infra:destroy": "bash scripts/infra-destroy.sh",
    "infra:bootstrap": "bash scripts/bootstrap-tfstate.sh",
    "infra:artifacts": "bash scripts/bootstrap-artifacts.sh",
    "infra:stub-lambdas": "bash scripts/upload-lambda-stubs.sh",
    "infra:upload-lambdas": "bash scripts/upload-lambdas.sh",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "dev:nominate": "node scripts/dev/enqueue-nomination.mjs",
    "dev:report": "node scripts/dev/invoke-report.mjs",
    "dev:users": "node scripts/dev/list-users.mjs",
    "dev:period": "node scripts/dev/inspect-period.mjs",
    "dev:recipient": "node scripts/dev/inspect-recipient.mjs",
    "dev:clear-eligibility": "node scripts/dev/clear-eligibility.mjs",
    "dev:reset-period": "node scripts/dev/reset-period.mjs",
    "dev:reminder": "node scripts/dev/invoke-reminder.mjs",
    "dev:tail": "node scripts/dev/tail-logs.mjs"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.630.0",
    "@aws-sdk/client-lambda": "^3.630.0",
    "@aws-sdk/client-secrets-manager": "^3.630.0",
    "@aws-sdk/client-sqs": "^3.630.0",
    "@aws-sdk/lib-dynamodb": "^3.630.0",
    "@eslint/js": "^9.9.0",
    "@types/node": "^20.14.10",
    "@typescript-eslint/eslint-plugin": "^8.4.0",
    "@typescript-eslint/parser": "^8.4.0",
    "adm-zip": "^0.6.0",
    "esbuild": "^0.28.1",
    "eslint": "^9.9.0",
    "eslint-config-prettier": "^9.1.0",
    "husky": "^9.1.7",
    "prettier": "^3.3.3",
    "tsx": "^4.23.1",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
