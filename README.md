# Documentation Package

This package contains repo-ready context for the Slack nomination bot. Copy `CLAUDE.md` and the `docs/` directory into the repository root.

The original research report is retained under `docs/research/`, while the numbered files and ADRs contain the approved implementation decisions.
