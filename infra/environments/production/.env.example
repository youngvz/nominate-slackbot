# Non-secret operator variables used by the deploy toolchain (not Terraform).
# See docs/08 §Environment configuration and docs/13 §Environment variables.
AWS_PROFILE=
AWS_REGION=us-east-1
TF_STATE_BUCKET=
TF_STATE_KEY=nominate-slackbot/production/terraform.tfstate
